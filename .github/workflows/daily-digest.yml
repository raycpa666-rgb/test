name: Daily hydrology news digest

on:
  schedule:
    # 13:07 UTC ≈ 6am Pacific / 7am Mountain.
    #
    # Deliberately :07 rather than :00. GitHub queues every top-of-the-hour
    # cron in the world at once, and this workflow's first scheduled run at
    # "0 13 * * *" was delivered 3.5 hours late (13:00 -> 16:30 UTC), which
    # turns a 6am briefing into a 9:30am one. Off-peak minutes are far less
    # contended. GitHub still makes no delivery-time guarantee for scheduled
    # workflows — see the README if you need a hard guarantee.
    - cron: "7 13 * * *"
  workflow_dispatch:
    inputs:
      send:
        description: "Send the email (otherwise draft only)"
        type: boolean
        default: false

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - name: Build and send digest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.send }}" != "true" ]; then
            python -m news_agent --quiet
          else
            python -m news_agent --send --quiet
          fi

      - name: Upload digest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: digest-${{ github.run_id }}
          path: |
            digest.txt
            digest.html
          if-no-files-found: ignore
